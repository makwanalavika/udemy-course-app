<div class="content messages_index">
  <% @messages.each do |message| %>
    <div class="summary">
      <p class="db_message"></p>
      <p id="new_message_<%= message.id %>"><%= message.user.username.capitalize %>: added  :<%= message.body %></p>
   </div>
  <% end %>
</div>
